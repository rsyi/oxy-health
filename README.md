# ddl version control
Because many of the tables I've created are based off of Google Sheets, I've included the DDL statements in the `ddl/` folder, organized by table name. I adhere to a standard where I don't use the same table name across databases/schemas, so these files should be unambiguous.

