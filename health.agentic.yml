llm:
  ref: claude
  max_tokens: 4096

context:
  - ./semantics/views/*.view.yml
  - ./semantics/topics/*.topic.yml
  - ./verified_queries/*

states:
  clarifying:
    max_retries: 3
  specifying:
    max_retries: 3
  solving:
    max_retries: 3
